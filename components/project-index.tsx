import projects from '../content/projects.json';
import indexes from '../content/indexes.json';
import type { Locale } from '../content/home';
import { SiteShell, pagePath, Arrow } from './site-shell';
export default function ProjectIndex({locale}:{locale:Locale}) {const t=indexes[locale].projects;return <SiteShell locale={locale} path="projects/"><section className="page-intro"><p className="eyebrow">{locale==='fr'?'Carnet de réalisations':'A collection of work'}</p><h1>{t.title}<span>.</span></h1><p className="intro">{t.intro}</p></section><div className="project-catalog">{projects.map((p,i)=><article className="catalog-entry" key={p.id}><div className="catalog-heading"><span className="index">{String(i+1).padStart(2,'0')} /</span><div><p className="eyebrow">{p.tags.join(' · ')}{p.year?' · '+p.year:''}</p><h2>{p.story?<a href={pagePath(locale,'projects/'+p.story+'/')}>{p.name}</a>:p.name}</h2></div></div><div className="catalog-copy"><p>{p[locale].summary}</p><p className="contribution">{p[locale].role}</p><p className="project-status">{p[locale].status}</p><div className="link-row">{p.story&&<a className="text-link" href={pagePath(locale,'projects/'+p.story+'/')}>{t.story} <Arrow/></a>}<a className="text-link" href={p.repo}>{t.code} <Arrow/></a>{p.upstream&&<a href={p.upstream}>{locale==='fr'?'Dépôt d’origine':'Upstream repository'} <Arrow/></a>}</div></div></article>)}</div></SiteShell>}
